namespace Utility.Messaging
{
    public enum MessageHeader
    {
        Unknown,
        Request,
        Response,
        Connection_Status,
        Public_Message,
        Private_Message,
        Server,
        Error
    }
}
